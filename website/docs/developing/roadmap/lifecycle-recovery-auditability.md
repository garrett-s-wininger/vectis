# Lifecycle, Recovery, And Auditability Tranche

Snapshot date: 2026-06-28.

This page tracks the initial tranche for graceful startup/shutdown, backup and
recovery, and auditability. It is a gap map, not a shipped-behavior reference.
When one of these items graduates into a contract, move the durable promise into
the operating docs, reference docs, or an ADR.

## Goals

| Goal | Definition of done |
| --- | --- |
| Graceful lifecycle | Every daemon has an explicit startup preflight, readiness/serving contract, drain behavior, bounded shutdown path, and test coverage for cancellation. |
| Backup and recovery | Operators can identify, capture, verify, restore, and smoke-test every durable state surface with machine-readable evidence. |
| Auditability | Security-relevant actions have explicit event coverage, clear durability semantics, query/export paths, retention policy, and health signals. |

## Current Strengths

| Area | What exists today |
| --- | --- |
| Shared signal context | `internal/cli.ExecuteWithShutdownSignals` gives Cobra daemon roots a SIGINT/SIGTERM-cancelled context. Most daemons use it. |
| HTTP drain | `vectis-api`, `vectis-cell-ingress`, and `vectis-docs` use bounded HTTP shutdown. API readiness flips to `503` while draining. |
| Worker drain | `vectis-worker` stops dequeuing on shutdown while allowing the active execution to finish finalization work. Tests cover shutdown during a run. |
| DB startup gate | DB-backed services call `OpenReadyDBForRole`, which waits for migrations instead of applying them at runtime. |
| Queue recovery | Queue persistence uses a per-shard lock, fsynced WAL records, snapshots, replay tests, and restore-skew reconciler tests. |
| Dispatch repair | The reconciler redispatches queued runs and pending continuations from durable SQL payloads, and repairs some orphaned task finalization cases. |
| Log durability | `vectis-log` has durable local run files; worker durable log streams spool during log-service outages; log-forwarder spools have CRC protection and quarantine paths. |
| Artifact durability | `vectis-artifact` stores content-addressed blobs, verifies SHA-256, and locks each local storage directory to one active writer. |
| Restore docs | Backup/restore, production drills, repair runbooks, health check catalog, and scaling/restart docs already describe much of the operator posture. |
| Audit foundation | API audit events have a central registry, docs catalog tests, SQL persistence, drop/flush metrics, and health checks for audit loss signals. |

## Gap Map

### Graceful Startup And Shutdown

| Gap | Why it matters | Likely fix |
| --- | --- | --- |
| gRPC shutdown fallback coverage is incomplete | The shared gRPC helper now marks health not-serving when a health server is supplied, calls `GracefulStop`, and forces `Stop` after a timeout. Remaining work is to make the timeout configurable where needed, add any missing health services, and avoid command-local gRPC stop paths outside the helper. | Finish the migration audit across worker-control and private callback servers, then promote the bounded helper behavior into the operating lifecycle contract. |
| Lifecycle behavior is unevenly encoded | `vectis-log-forwarder` now relies on the Cobra shutdown context, and worker-core, artifact, and log gRPC serving paths use the shared helper. Other background loops still use local stop/join conventions. | Consolidate daemon lifecycle helpers and add focused cancellation-return tests for long-running loops. |
| Readiness/drain is mostly API-specific | API readiness turns unhealthy during drain and checks DB/queue. gRPC health services generally report serving once started and do not model dependency loss or drain. Background-loop daemons have no readiness endpoint. | Define per-binary readiness: serving, draining, dependency-unready, and startup-preflight-failed. Add gRPC health transitions where applicable. |
| Startup preflight contract needs deeper enforcement | The operating reference now has a per-daemon lifecycle matrix, a static test requires every daemon command to appear, and the Podman renderer rejects shared-pod TCP listener collisions plus Prometheus targets that point at the wrong service listener. Remaining work is to test more of the matrix fields against command wiring and to promote dependency-readiness checks into machine-readable health where appropriate. | Expand contract tests beyond row coverage: startup preflight, health surface, drain helper, and durable state expectations. |
| Worker termination grace is operational, not enforced | Workers intentionally let active work finish, but supervisor grace periods are not derived from worker config or surfaced as a contract. | Document and expose recommended grace windows; add metrics/log messages for time spent in drain/finalize. |
| Background loops lack a common stop/join pattern | Catalog, reconciler, cell-ingress repair, TLS reload loops, source sync, log spool forwarding, and registry gossip each use local patterns. | Add small lifecycle tests or helper contracts for "context cancellation returns within N." |

### Backup And Recovery

| Gap | Why it matters | Likely fix |
| --- | --- | --- |
| Backup inventory needs deployment breadth | `vectis-cli backup inventory --format json` now emits local backup scope evidence, and `vectis-cli backup manifest` aggregates one or more host inventories into backup-set evidence. `vectis-cli backup verify --expect` can make missing expected inventory sources, service instances, database roles, paths, or categories fail. `vectis-cli backup expect podman` generates expectations for Podman simple/HA, and `vectis-cli backup expect linux` generates a Linux services-manifest baseline. Remaining work is deploy-specific dashboard inventory and arbitrary config-manager topology generation. | Add config-manager expectation generation for required hosts, cells, shards, dashboards, and externally managed secret/TLS stores. |
| Backup set manifest verifier needs custom topology inputs | `vectis-cli backup verify` now validates manifest shape, core database/queue/log/artifact evidence, dirty schema markers, missing or unreadable required paths, optional expected-topology JSON, generated Podman profile expectations, and generated Linux services-manifest baseline expectations. It does not yet generate expectations from arbitrary config-manager topology. | Add import/generation paths for Ansible/Terraform/Kubernetes inventory or a Vectis-native topology file. |
| No first-party backup/restore helpers | `vectis-cli database migrate`, health checks, and retention exist; backup/restore execution is delegated to the platform. | Start with inventory and verification. Consider SQLite-safe backup helpers later; keep Postgres backup delegated to the DB platform. |
| Restore drill is mostly manual | A focused local restore drill test now backs up and restores SQLite plus queue, log, artifact, log-forwarder spool, and worker pending log-spool paths; reopens restored state; reruns migrations; emits restored inventory, manifest, and storage reports; and requires the combined verification through `backup verify --storage-report`. A local e2e restore drill now starts the stack on isolated ports, runs the canonical smoke job, copies the data home as backup media, verifies restored backup evidence, restarts from the restored data home, confirms the pre-restore run/logs/artifacts, runs a post-restore smoke job, and emits `backup restore-validation` JSON tying the restored manifest and storage reports to that smoke run. A Podman reference e2e restore drill now stops the reference stack, exports and imports the Postgres, queue, log, artifact, secret, and SPIFFE volumes as platform backup media, recreates the Podman secret from the deploy config directory, restarts services, confirms the pre-restore smoke run/log/artifact, and runs a post-restore secret/log/artifact smoke job. Remaining work is production-grade external DB backup media and HA/config-manager restore coverage. | Extend the reference deployment drill to external Postgres backup/restore procedures, HA profile expectations, and config-manager-managed secrets/TLS/config. |
| File-store integrity checks need drill automation | `vectis-cli storage verify` now checks queue snapshot/WAL files, durable run logs, artifact blob SHA-256 path digests, log-forwarder CRC spools, and worker pending log spools. `backup verify --storage-report` can require matching fresh `ok` reports for every storage-backed local-state path, `backup restore-validation` emits retained validation evidence with storage verification plus post-restore smoke status, retention cleanup can require the same validation before deletion, and the local CLI plus e2e restore drills exercise this validation path after restore. The Podman reference e2e drill stores expected-topology evidence, volume archive hashes, and pre/post restore smoke run IDs. Remaining work is production-readiness gate integration and reference-deployment validation retention through first-party manifests/storage reports. | Add reference-deployment restore-validation automation that stores the combined storage-report, backup-manifest, and smoke-run validation artifact once platform inventory can describe restored volumes. |
| Worker pending log spool is not fully operationalized | Worker durable log streams replay pending spools, backup inventory records the default temp-backed pending path, and `vectis-cli storage verify worker-log-spool` can validate pending files. Remaining work is making the path configurable and observable as a first-class operator surface. | Make the worker log spool path configurable, add metrics/health for pending/quarantined files, and include it in deployment-specific backup expectations. |
| Retention compliance controls are only partially linked | `vectis-cli retention cleanup` now consumes `VECTIS_RETENTION_CLEANUP_*` deployment defaults for cleanup windows, evidence freshness, mandatory backup/audit/hold-review gates, and an optional cleanup evidence manifest path. It can verify retained waiver evidence for approved exceptions. `vectis-cli retention holds` can protect selected terminal runs plus related run state or `audit_log` time ranges from cleanup, `retention holds review` emits retained active-hold inventory evidence, `retention evidence manifest --verify` checks evidence before writing and atomically promoting the cleanup evidence manifest, `retention evidence metrics` publishes retained evidence as Prometheus textfile metrics, and `retention scheduled-cleanup` now sequences audit export, hold review, manifest verification/promotion, cleanup, and reference evidence metrics as one observable CLI workflow. Reference Linux artifacts and packages ship a timer-driven scheduled cleanup unit that retains the workflow JSON output and `.prom` evidence metrics under `/var/lib/vectis/ops`. Remaining work is production storage-report handoff and environment-specific threshold tuning. | Add production runbooks for storage-report collection, restore-validation retention cadence, and site-specific alert thresholds. |
| Partial restore contracts are not executable | The docs describe partial restore outcomes, but there are few automated assertions for each expected inconsistency. | Add tests for database-only, queue-only, missing log store, missing artifact store, and missing secret/TLS restore postures. |

### Auditability

| Gap | Why it matters | Likely fix |
| --- | --- | --- |
| `fail_closed` route enforcement is not yet atomic | Identity, setup, token, password, and namespace-binding mutations now return an API error when their fail-closed audit event cannot be persisted. Remaining work is deciding whether these domain mutations and audit inserts need a shared transaction so audit failure also rolls back the mutation instead of only suppressing the success response. | Move fail-closed mutations into transactionally coupled audit writes where the repository boundary supports it, or document the post-mutation repair contract explicitly for each event. |
| Audit query/export is only an MVP | Admin-only `GET /api/v1/audit/events`, `vectis-cli audit list --format json`, and `vectis-cli audit export` now cover newest-first audit review, cursor pagination, retained export evidence, and an optional retention gate. | Add CSV output if operators need it, signed export manifests, and policy checks that require audit-range export evidence before deleting old rows. |
| A few non-GET routes are intentionally outside `audit_log` | The route inventory now has a static audit coverage test that requires every non-GET route to declare expected audit events or an explicit unaudited rationale. Source schedule/override mutations, source repository sync commands, and logout session revocation now emit audit events. Remaining unaudited routes are definition-resolution preview and catalog fan-in ingestion through `cell_catalog_events`. | Promote the unaudited rationale into a durable evidence-model contract, and keep adding explicit route matrix entries as new API surfaces land. |
| Some operational/security evidence is outside `audit_log` | Dispatch events, execution security events, access logs, API security rejections, and audit events are separate surfaces. That is reasonable, but the joining contract is not centralized. | Document the evidence model: which table/log/metric answers which question, and how correlation IDs, run IDs, actor IDs, and dispatch events join. |
| Process lifecycle is not audited | Service startup, shutdown, readiness drain, migration wait, and restore/retention operations are not consistently recorded as auditable events. | Add structured service lifecycle logs first; consider DB-backed operator events only where a human-triggered command changes durable state. |
| Audit retention can still erase evidence unless operators opt into the gate | Retention deletes old unheld `audit_log` rows by policy. `vectis-cli audit export` can now produce retained evidence, `VECTIS_RETENTION_CLEANUP_REQUIRE_AUDIT_EXPORT=true` makes cleanup verify export evidence before deleting eligible audit rows, audit-range holds can preserve selected review windows, `retention holds review` can record the reviewed active hold inventory, `VECTIS_RETENTION_CLEANUP_REQUIRE_HOLD_REVIEW=true` makes that inventory review mandatory, `retention scheduled-cleanup` can generate audit/hold evidence, promote the cleanup manifest, run cleanup as one observable job, and publish retained evidence metrics, `--waiver` records approved exceptions, and reference Linux deployments retain the scheduled workflow JSON plus `.prom` evidence metrics. Remaining work is production failure reporting beyond starter alerts. | Add production runbooks for failed scheduled cleanup evidence and waiver escalation. |

## First Work Packages

| Package | Scope | Suggested tests |
| --- | --- | --- |
| Lifecycle contract matrix | Initial operating reference table landed for each daemon's startup checks, readiness, drain behavior, state closed on exit, and hard-stop fallback. | Static test requires every `cmd/*` daemon except `vectis-cli` to appear in the matrix. |
| Bounded gRPC shutdown | Shared helper landed for queue, registry, orchestrator, log, artifact, secrets, worker-control, and worker-core serving paths. | Unit test stuck-stream fallback; add command tests where the command has meaningful shutdown-owned state. |
| Log-forwarder lifecycle cleanup | Custom signal handling removed from `runLogForwarder`; shutdown now follows `cmd.Context()`. | Command-level test with cancelled context; existing forwarder shutdown tests. |
| Backup inventory MVP | `vectis-cli backup inventory --format json` landed for schema visibility, redacted DB role DSNs, queue/log/artifact/spool paths, secret/TLS path visibility, and service instance IDs. | CLI tests cover JSON evidence and DSN redaction; add split global/cell DB config coverage next. |
| Backup manifest MVP | `vectis-cli backup manifest --format json` builds manifest evidence from host inventories, and `vectis-cli backup verify` fails on missing core state, unreadable paths, and dirty schema markers. | CLI tests cover manifest aggregation and missing required path failure. |
| Backup expected topology MVP | `vectis-cli backup verify --expect expected-topology.json` landed for required inventory sources, service instances, database roles, paths, and categories. | CLI tests cover expected topology success and missing expected source/instance/path failures. |
| Podman backup expectation MVP | `vectis-cli backup expect podman --profile simple|ha --format json` landed for reference deployment queue/log/artifact shards, Postgres roles, secret store evidence, and config-path category checks. | CLI test covers HA generated expectations and verifier compatibility. |
| Linux backup expectation MVP | `vectis-cli backup expect linux --manifest deploy/linux/services.toml --format json` landed for service-manifest queue/log/artifact/log-forwarder paths, Postgres roles, encryptedfs secret store paths, cron/queue/log/artifact instance IDs, and `/etc/vectis` config evidence. | CLI test covers generated Linux expectations and verifier compatibility. |
| Retention backup gate MVP | `vectis-cli retention cleanup` can verify a backup manifest, optional expected topology, optional max manifest age, and optional storage integrity reports before dry-run or apply; output includes backup validation fields when supplied. | CLI tests cover missing manifest flag combinations, failed manifest verification, stale manifest rejection, fresh expected-topology success, fresh storage reports, and stale storage report rejection. |
| Run-scoped retention hold MVP | `vectis-cli retention holds create/list/release` landed for compliance/legal/incident holds. Active holds skip terminal run cleanup for the run row, dispatch events, artifact manifests, task graph rows, local run logs, and artifact reference removal; create/release writes audit rows. | Retention tests cover hold create/list/release audit events, held cleanup counts, skipped terminal deletion, protected child rows, and protected artifact references. |
| Audit-range retention hold MVP | `vectis-cli retention holds create --audit-since --audit-until` landed for compliance/legal/security review windows. Active audit-range holds skip matching `audit_log` rows while normal audit retention continues outside the held range; cleanup output reports `held.audit_log`. | Retention tests cover held audit row counts, preserved held audit rows, deleted unheld audit rows, and CLI target validation. |
| Retention policy gate MVP | `vectis-cli retention cleanup` landed `--require-backup-manifest`, `--require-audit-export`, `--require-hold-review`, and retained `--waiver` evidence. Required backup gates fail without verified manifest evidence or a waiver. Required audit gates fail only when audit rows are eligible for deletion and export evidence is missing or unverified. Required hold-review gates fail without retained active-hold review evidence or a waiver. | CLI tests cover required backup failure, waiver validation and rejection, required audit-export failure, required hold-review failure, inventory mismatch rejection, and waiver allowance. |
| Retention policy defaults MVP | `retention.cleanup.*` / `VECTIS_RETENTION_CLEANUP_*` landed for cleanup windows, backup/audit/hold-review evidence freshness, mandatory backup/audit/hold-review cleanup gates, and an optional cleanup evidence manifest path. `vectis-cli retention cleanup` uses these as command defaults while one-off flags still override them. | Config tests cover default and overridden cleanup policy values; CLI tests cover consuming the config defaults. |
| Retention hold-review evidence MVP | `vectis-cli retention holds review` landed a retained `vectis.retention_hold_review.v1` evidence envelope with reviewer, reason, optional external reference, active hold count, canonical hold inventory, and SHA-256 hash. Cleanup can verify this evidence with `--hold-review`, freshness-gate it with `--hold-review-max-age`, and require it with `--require-hold-review`. | CLI tests cover evidence hashing, fresh matching inventory, stale review rejection, changed active-hold inventory rejection, cleanup report fields, and waiver allowance. |
| Retention cleanup evidence manifest MVP | `vectis-cli retention cleanup --evidence-manifest` landed a retained `vectis.retention_cleanup_evidence.v1` contract for scheduled cleanup jobs to name backup manifest, expected topology, storage reports, audit export, hold review, waiver, freshness, and required-gate inputs. Relative paths resolve beside the manifest, and explicit CLI flags override individual manifest fields. | CLI tests cover manifest option application, relative path resolution, explicit flag precedence, schema rejection, config defaults, and cleanup output reporting. |
| Retention evidence manifest generation MVP | `vectis-cli retention evidence manifest` landed for writing the same retained cleanup evidence contract and atomically promoting a stable manifest path after backup, audit export, and hold review evidence has been produced. `--verify` runs cleanup-equivalent backup, audit export, hold review, waiver, and required-gate checks before writing or promoting. | CLI tests cover generated manifest shape, invalid backup option rejection, verification success and stale evidence rejection, receipt output, file permissions, verification receipt fields, and promoted payload parity. |
| Retention scheduled cleanup workflow MVP | `vectis-cli retention scheduled-cleanup` landed for scheduled operators: it can export audit evidence, write active hold review evidence, verify backup/audit/hold/waiver gates, promote the cleanup evidence manifest, and run cleanup through the promoted manifest in one command. | CLI tests cover retained evidence generation, manifest promotion, verification object output, and dry-run cleanup JSON wrapping. |
| Retention evidence metrics MVP | `vectis-cli retention evidence metrics` landed for Prometheus textfile output from retained scheduled-cleanup, backup manifest, restore-validation, storage report, audit export, hold-review, and waiver JSON evidence. Reference Linux scheduled cleanup writes `/var/lib/vectis/ops/retention-evidence.prom`, starter alerts consume the metric family, and the bundled dashboard includes backup/retention evidence panels. | CLI tests cover generated text metrics and input validation; deploy tests cover the reference timer output path. |
| Storage integrity verifier MVP | `vectis-cli storage verify` landed for artifact, logs, queue, log-forwarder spool, and worker pending log-spool surfaces. Reports include checked files/bytes, record counts, warnings, errors, and JSON output for evidence capture. | Storage verifier tests cover digest mismatch, run-log truncation, corrupt queue WAL payloads, log-forwarder CRC failure, worker spool frame mismatch, and CLI JSON/text report behavior. |
| Storage report manifest gate MVP | `vectis-cli backup verify --storage-report` landed for attaching storage verifier JSON to backup manifest checks. It requires every storage-backed `local_state` path in the manifest to have a matching `ok` report and can reject stale reports with `--storage-max-age`. | Backup verifier tests cover matching storage reports, missing report failures, and retention cleanup reuse of fresh/stale storage validation. |
| Restore drill MVP | Focused local restore drill landed for SQLite plus queue/log/artifact/spool file state. It restores into a new data root, reruns migrations, reopens restored DB/log/artifact state, emits restored inventory and storage reports, and gates the manifest with those reports. The local e2e drill now runs the live stack before and after restore, proves run/log/artifact continuity plus post-restore execution, and stores `backup restore-validation` JSON. The Podman reference e2e drill now proves stopped-stack volume export/import media, restored run/log/artifact access, generated secret reuse, and post-restore execution. | CLI package test covers the local validation path; local e2e covers live restore smoke and retained validation generation; Podman e2e covers reference deployment media restore. Add retained reference-deployment `restore-validation` once platform inventory exists. |
| Podman startup listener contract | Podman simple and HA render tests now require every declared TCP listener in the shared pod to be unique, and every Prometheus scrape target to resolve to a listener owned by the intended service container. Reconciler metrics are explicitly declared so static checks see the same startup surface the process binds. | CLI render tests cover simple and HA listener uniqueness plus Prometheus target ownership. |
| Audit durability enforcement MVP | Fail-closed setup and token routes now have route-level tests proving audit persistence errors produce API errors instead of successful responses; identity and namespace-binding handlers use the same enforcement helper. | Add transactional repository tests if/when fail-closed mutations are coupled with audit inserts. |
| Audit route coverage MVP | Route inventory tests now require every non-GET route to map to registered audit event types or carry an explicit unaudited rationale. Source schedule/override, source sync, and logout routes now have first-class audit event types. | Extend the matrix as new routes land; add evidence-model docs for routes intentionally outside `audit_log`. |
| Audit export MVP | Admin API and CLI list/export for `audit_log` landed with cursor pagination, bounded filtering, JSON evidence output, event hashing, truncation detection, optional retention cleanup verification through `--audit-export`, and audit-range hold support. | Add optional CSV output, signed export manifests, and mandatory policy hooks. |

## Contract Promotion Checklist

Before marking a gap closed:

- The behavior is implemented in code and not only described in docs.
- The behavior has at least one focused test and, for operator flows, a health/drill evidence path.
- The user-facing or operator-facing contract is documented in the durable docs.
- The failure mode says what data may be lost, what is repairable, and what cannot be reconstructed.
- Metrics, logs, audit rows, or CLI output let an operator prove the behavior happened.
- The release evidence template or drill checklist names the new validation when it matters for production readiness.

## Related Docs

| Need | Doc |
| --- | --- |
| Current planning home | [Planning](./planning.md) |
| Restart behavior | [Scaling And Restarts](../../operating/deployment/scaling-and-restarts.md) |
| Backup and restore | [Backup, Restore, And Disaster Recovery](../../operating/reliability/backup-restore.md) |
| Production drills | [Production Drills](../../operating/reliability/production-drills.md) |
| Health checks | [Health Check Catalog](../../operating/reference/health-check-catalog.md) |
| Audit events | [Audit Event Catalog](../../operating/reference/audit-event-catalog.md) |
| CLI coverage | [CLI Operational Coverage](../../operating/reference/cli-operational-coverage.md) |
