edition = "2023";

import "common.proto";

option go_package = "vectis/api/gen/go;api";

service WorkerControlService {
    rpc CancelRun(CancelRunRequest) returns (common.Empty);
}

message CancelRunRequest {
    string run_id = 1;
    string cancel_token = 2;
}
